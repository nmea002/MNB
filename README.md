#Steps 
0. Must have dataset downloaded. 
1. Rename the dataset to 'datasetA.csv'
2. Create a folder in the project folder called 'dataset'
3. Put the dataset in there
4. I think you should be able to run it with the venv but if you can't, run the following in the terminal:
- pip install -r requirements.txt
